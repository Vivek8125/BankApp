import { TransactinFilterPipe } from './transactin-filter.pipe';

describe('TransactinFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactinFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
